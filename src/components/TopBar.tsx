import React from 'react';

const TopBar = () => {
  return (
    <div className="relative bg-[#f5f1eb] text-gray-800 py-1 px-4 text-center z-40 border-b border-gray-200/50">
      <p className="text-xs md:text-sm font-semibold">
        Le fichier sur le thème du FOOTBALL le plus complet du marché.
      </p>
    </div>
  );
};

export default TopBar;