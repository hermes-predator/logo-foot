import React from 'react';

const TopBar = () => {
  return (
    <div className="relative bg-gradient-to-br from-white/85 via-gray-50/40 to-white/70 text-gray-800 py-1 px-4 text-center z-40 border-b border-gray-200/50">
      <p className="text-[9px] md:text-[10px] font-semibold">
        Vous obtenez le fichier sur le thème du FOOTBALL le plus complet grâce à logo-foot.com
      </p>
    </div>
  );
};

export default TopBar;